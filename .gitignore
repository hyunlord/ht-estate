# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.pyright/

# ── Node / Next.js ──
node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo
next-env.d.ts

# ── SQLite (로컬 저장소 — 적재 산출물) ──
*.db
*.db-journal
*.sqlite3
# apps/api/data/ 적재 DB는 *.db로 무시되고, data/seeds/ 시드는 추적된다.

# ── Playwright ──
test-results/
playwright-report/
playwright/.cache/

# ── 환경/시크릿 ──
.env
.env.*
!.env.example

# ── macOS ──
.DS_Store

# ── OMC 오케스트레이션 상태 (개인용) ──
.omc/
